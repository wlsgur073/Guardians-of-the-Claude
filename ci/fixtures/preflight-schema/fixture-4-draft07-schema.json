{
  "$comment": "Fixture 4 — CONTROL: draft-07 mislabeled schema stub. This is NOT a profile instance — it is a schema document whose $schema declaration has been manually rewritten to draft-07 (pre-2019-09). The real v1.1.0 schema does not yet exist (it is a T1 deliverable). This stub mimics the intended v1.1.0 schema shape structurally but uses the draft-07 $schema URI. Purpose: when the probe runs assertions 2 + 3 (fixture-2 and fixture-3) against THIS stub schema instead of the real draft-2020-12 schema, the validator must ACCEPT both fixtures — proving that unevaluatedProperties enforcement is draft-2020-12-dependent. This is the probe's control confirmation that the $schema declaration on the real wrapper schema is load-bearing.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Project Profile (v1.1.0 stub — draft-07 mislabel control)",
  "type": "object",
  "required": ["schema_version", "metadata"],
  "unevaluatedProperties": false,
  "properties": {
    "schema_version": { "const": "1.1.0" },
    "metadata": {
      "type": "object",
      "required": ["generated_by", "last_updated"],
      "properties": {
        "generated_by": { "const": "guardians-of-the-claude" },
        "last_updated": { "type": "string", "format": "date-time" },
        "source_files_checked": { "type": "array", "items": { "type": "string" } }
      },
      "unevaluatedProperties": false
    },
    "runtime_and_language": {
      "type": ["object", "null"],
      "properties": {
        "runtime": { "type": ["string", "null"] },
        "language": { "type": ["string", "null"] },
        "module_system": { "type": ["string", "null"] }
      },
      "unevaluatedProperties": false
    },
    "framework_and_libraries": {
      "type": ["object", "null"],
      "properties": {
        "framework": { "type": ["string", "null"] },
        "ui": { "type": ["string", "null"] },
        "styling": { "type": ["string", "null"] }
      },
      "unevaluatedProperties": false
    },
    "package_management": {
      "type": ["object", "null"],
      "properties": {
        "manager": { "type": ["string", "null"] },
        "lock_file": { "type": ["string", "null"] }
      },
      "unevaluatedProperties": false
    },
    "testing": {
      "type": ["object", "null"],
      "properties": {
        "unit": { "type": ["string", "null"] },
        "e2e": { "type": ["string", "null"] }
      },
      "unevaluatedProperties": false
    },
    "build_and_dev": {
      "type": ["object", "null"],
      "properties": {
        "bundler": { "type": ["string", "null"] },
        "linter": { "type": ["string", "null"] },
        "formatter": { "type": ["string", "null"] }
      },
      "unevaluatedProperties": false
    },
    "project_structure": {
      "type": ["object", "null"],
      "properties": {
        "type": { "enum": ["single_project", "monorepo", null] },
        "source_convention": { "type": ["string", "null"] },
        "key_directories": { "type": "array", "items": { "type": "string" } }
      },
      "unevaluatedProperties": false
    },
    "claude_code_configuration_state": {
      "type": ["object", "null"],
      "properties": {
        "claude_md": { "type": "object" },
        "settings_json": { "type": "object" },
        "rules_count": { "type": "integer", "minimum": 0 },
        "agents_count": { "type": "integer", "minimum": 0 },
        "hooks_count": { "type": "integer", "minimum": 0 },
        "mcp_servers_count": { "type": "integer", "minimum": 0 },
        "model": { "type": "string" },
        "scoring_model_ack": {
          "type": "object",
          "properties": {
            "version": {},
            "seen_count": { "type": "integer", "minimum": 0 }
          },
          "unevaluatedProperties": false
        }
      },
      "unevaluatedProperties": false
    }
  }
}
