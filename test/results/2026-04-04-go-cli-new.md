---
scenario: go-cli-new
date: 2026-04-04
path: starter
language: Go 1.24
framework: Cobra v1.9.1
model: claude-opus-4-6
score: 67
grade: B
gate: READY
maturity: "Level 1 — Basic"
---

# Eval: go-cli-new

## Summary

Generated starter-path configuration for a new Go CLI project using Cobra v1.9.1. The output correctly identifies the project structure (`main.go` -> `cmd.Execute()` -> `cmd/root.go`), references only directories that exist (`cmd/`), and produces copy-pasteable Go commands. The main gap is the absence of security rules and incomplete deny coverage in settings.json, which is expected behavior for the starter path but pulls down the Protection (T2) score.

## Run Details

### Generated Files

| File | Lines | Notes |
|------|-------|-------|
| `CLAUDE.md` | 38 | 6 sections, all required content present |
| `.claude/settings.json` | 14 | allow: go test/build/run, deny: .env/.env.* |
| `.gitignore` | 5 | Binary (`mycli`) + `.claude/settings.local.json` |

### Eval Dimensions (1-5)

| Dimension | Score | Notes |
|-----------|-------|-------|
| Accuracy | 5 | Correct Go 1.24, Cobra v1.9.1, actual file/function names, no phantom directories |
| Customization | 4 | References `mycli`, `cmd.Execute()`, Cobra pattern; could also reference `rootCmd` variable name or `Short` description |
| Completeness | 4 | All starter-path deliverables present; deny list covers `.env` but not `secrets/` |
| Conciseness | 5 | 38 lines, no filler content |
| Best Practices | 5 | Commands copy-pasteable, paths verified, project-specific details included, under 200 lines |
| **Average** | **4.6** | |

### Audit Score Breakdown

```
Quality Gate: READY    (CLAUDE.md OK, test command OK)

Foundation Gate (FG):  1.00    ==================== 100%
  CLAUDE.md existence   PASS (1.0) x 0.25
  Test command          PASS (1.0) x 0.35
  Build command         PASS (1.0) x 0.20
  Project overview      PASS (1.0) x 0.20

Protection (T2):       0.32    ======.............. 32%
  Sensitive file prot.  PARTIAL (0.6) x 0.40  — .env covered, no secrets/
  Security rules        FAIL (0.0) x 0.35     — none (starter path omits rules/)
  Hook config quality   SKIP                  — no hooks

Optimization (T3):     1.00    ==================== 100%
  Directory references  PASS (1.0) x 0.20  — cmd/ verified
  CLAUDE.md length      PASS (1.0) x 0.10  — 38 lines
  Command availability  PASS (1.0) x 0.20  — go.mod present
  Rules path validation SKIP               — no rules/
  Agent config quality  SKIP               — no agents/
  MCP configuration     SKIP               — no DB/API deps

Detail Score (DS):     59.2    (T2: 0.32 x 60% + T3: 1.00 x 40%) x 100
Synergy: +2  (test + build both PASS)
LQM: +6  (overview 3 + commands 2 + patterns 1)

Score: 67/100 (Grade: B)  |  Maturity: Level 1 — Basic

  Breakdown:
  FG: 0.15 + 0.85 x 1.00 = 1.00
  DS: (0.32 x 0.60 + 1.00 x 0.40) x 100 = 59.2
  SB: +2 (test + build)
  LQM: +6 (overview 3 + commands 2 + patterns 1)
  Final: 1.00 x 59.2 + 2 + 6 = 67.2 -> 67
```

### Why Not Higher

- **T2 Protection is low (0.32)** because the starter path intentionally skips `.claude/rules/` and security rule files. This is by design -- the template says "Do NOT generate .claude/rules/, hooks, agents, or skills on the starter path." The deny list also only covers `.env` patterns, not `secrets/`.
- **Maturity stuck at Level 1** because T2_Score (0.32) < 0.6 required for Level 2. Again, a structural limitation of the starter path.
- **LQM patterns scored 1/3** because Cobra command patterns (`AddCommand` + `init()`) are fairly well-known, not truly non-obvious gotchas.

### What Went Well

- Perfect foundation (FG = 1.0) -- all four T1 items PASS
- Perfect optimization (T3 = 1.0) -- all non-SKIP items PASS
- CLAUDE.md references concrete code details: `cmd.Execute()`, `cobra.Command`, `Use: "mycli"`, `cmd/root.go`
- No phantom directories -- does not assume `internal/`, `pkg/`, or `test/` exist
- .gitignore created with binary name matching the Cobra `Use` field

## LLM Context Note

This eval was run by the same LLM that generated the output (self-eval). The mechanical audit scoring (FG, T2, T3, DS, SB) is deterministic and based on file existence checks. The subjective components are the 5-dimension ratings and LQM (max 8 points). To reduce bias, LQM used the constrained 3-level/binary format from the scoring model. The 5-dimension scores were anchored to specific observable evidence listed in the Notes column.
