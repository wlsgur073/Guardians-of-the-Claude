# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/

# VS Code
.vscode/
*.code-workspace
.history/
*.vsix

# Claude Code (local dev config — not distributed with plugin)
.claude/*

# Internal plans, design specs, and eval tests (not user-facing)
/test/
docs/superpowers/

# Smoke hook ephemeral lock (created by session-start.{sh,ps1} mkdir-lock;
# normally removed by its EXIT trap — guard against a SIGKILLed leftover)
ci/fixtures/**/.session-start.lock/

# Node (if contributors install anything for tooling)
node_modules/

# Python (CI scripts cache)
__pycache__/
*.pyc
.pytest_cache/

# Obsidian
.obsidian/

# Release assets (generated by ci/scripts/build-manifest.{sh,ps1}; uploaded to GitHub release)
/eval-manifest.json